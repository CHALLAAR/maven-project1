apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy12
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kuard
  template:
    metadata:
      labels:
        app: kuard
    spec:
      containers:
      - name: kuard
        image: nginx:1.20
